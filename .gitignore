_build/
_opam/
*.install
*.merlin

# Benchmark and test logs written by `make system` / `make record`.
*.log

# Internal work orders and the staging area for tests written ahead of
# their implementation. Kept locally; not part of the published library.
tasks/
test/staged/

# Local editor and agent state.
.claude/
