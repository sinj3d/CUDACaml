; GATED. These only mean something once every unit test is green and a GPU
; is present. `make test` enforces the ordering; the binary itself also
; refuses to run unless CUDACAML_SYSTEM=1 is set.
(tests
 (names test_system)
 (libraries cudacaml cudacaml_examples cudacaml_testlib unix))
